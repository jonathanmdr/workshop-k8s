apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
  namespace: apps
spec:
  type: NodePort
  ports:
    - port: 80
      nodePort: 30000
      name: http-apps
      protocol: TCP
  selector:
    app.kubernetes.io/name: ingress-nginx
